# dependencies
node_modules/

# expo
.expo/
dist/
web-build/
*.orig.*
.expo-shared/

# native
ios/
android/
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# misc
.DS_Store
*.log
# env files are never committed — build-time config lives in eas.json / render.yaml-style files
.env*
.idea/
.vscode/

# bun
# (bun.lock is committed; lockb binary is also fine to commit)
